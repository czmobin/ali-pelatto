# ============================================================
# state موقت درون‌حافظه‌ای (با ری‌استارت ربات پاک می‌شود)
# این دیکشنری‌ها فقط در جای خود mutate می‌شوند، هرگز reassign نمی‌شوند،
# تا بین همه‌ی ماژول‌ها یک شیء مشترک بمانند.
# ============================================================

voice_call_cooldown = {}   # {str(user_id): timestamp}
otp_cache = {}             # {str(user_id): otp}
temp_purchase_data = {}    # {buyer_id: {...}}
temp_group_links = {}      # {ad_id: {...}}
